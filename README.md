# Smart Route Optimization Platform

Static Next.js app — deploys to AWS Amplify.

## Run locally

```bash
npm install
npm run build
npm start
```

Or dev: `npm run dev`

## Deploy (AWS Amplify)

1. Connect this repo to Amplify (GitHub).
2. Branch: `main`.
3. Build: auto-detected (Next.js static export).
4. Output: `out`.

Live URL after deploy: `https://main.<app-id>.amplifyapp.com`
